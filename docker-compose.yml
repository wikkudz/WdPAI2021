version: '3'

services:
  web:
    build:
      context: docker2
      dockerfile: docker/nginx/Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/app/
    links:
      - php

  php:
    build:
      context: docker2
      dockerfile: docker/php/Dockerfile
    volumes:
      - .:/app/
